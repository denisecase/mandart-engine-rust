# Folder containing .mandart files - change to input to run all
input_folder = "input_small"

# Folder containing reference outputs from MandArt
check_folder = "input_swift"

# Folder where engine output PNGs and CSVs will be saved
output_folder = "output"

# Whether to save the grid as CSV (true/false)
save_grid = true

# Paths for default files (used for preloading)
default_csv_path = "assets/MandArt_Catalog/Default.csv"
default_mandart_path = "assets/MandArt_Catalog/Default.mandart"
